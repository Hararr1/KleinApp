<UserControl x:Class="KleinMessage.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KleinMessage.Views"
             mc:Ignorable="d" 
             d:DesignHeight="710" d:DesignWidth="600" 
             xmlns:cal="http://www.caliburnproject.org" Background="Azure">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>




            <!--<Border CornerRadius="0 20 20 0" Grid.RowSpan="2">
                <Border.Background>
                    <LinearGradientBrush>
                        <GradientStop Color="#EFEFEF" Offset="0.0"/>
                        <GradientStop Color="#52FFEE" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>-->

            <StackPanel Margin="0 40 0 0">
                <Image Source="/Assets/logo.png" Width="150"/>
                <TextBlock
                    x:Name="FirmNameTextBlock"
                    Text="KleinApp" FontFamily="Sitka Text"
                               FontWeight="Light" FontSize="40" 
                                HorizontalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Row="1" Margin="80 0 80 0">
                <StackPanel Orientation="Horizontal" Margin="50 20 50 0">

                    <TextBox 
                         x:Name="UsernameTextBox"                    
                         FontFamily="Sitka Text"                     
                         FontWeight="Light" 
                         FontSize="20"
                         Background="Transparent"
                         BorderThickness="0"
                         HorizontalAlignment="Center"
                         Width="300"
                         MaxLength="20"
                         Opacity="0.6" GotFocus="ClickEvent" />
                    <Image 
                         Source="/Assets/username.png" Width="20"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Center"
                         Opacity="0.5"
                         Margin="10 0 0 5"/>
                </StackPanel>
                    <Label 
                         x:Name="UsernameLabel" Height="2"
                         Background="Black"
                         Opacity="0.5"
                         Margin="50,5,50,0"/>



                <StackPanel Orientation="Horizontal" Margin="50 20 50 0">

                    <PasswordBox 
                        x:Name="PasswordTextBox" 
                        FontFamily="Sitka Text"  
                        FontWeight="Light"
                        FontSize="20"
                        Background="Transparent"
                        BorderThickness="0"
                        HorizontalAlignment="Center"
                        Width="300"
                        Opacity="0.6" 
                        MaxLength="20"
                        GotFocus="ClickEventPassword"                    
                        cal:Message.Attach="[Event KeyDown] = [Action IsEnterClick($eventArgs)]"
                        />

                    <Image 
                       Source="/Assets/padlock.png" Width="20"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Opacity="0.5"
                       Margin="10 0 0 0"/>
                </StackPanel>
                <Label  
                        x:Name="PasswordLabel" Height="2"
                        Background="Black"
                        Opacity="0.5" Margin="50,5,50,0"/>
                <TextBlock x:Name="RequestMessage"
                           HorizontalAlignment="Center"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           Visibility="{Binding IsErrorVisible, 
                    Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                             />


                <StackPanel Orientation="Horizontal" Margin="50 50 50 0">
                    <Button x:Name="LogIn" Style="{StaticResource RoundButtonStyle}" Width="140" Height="50" Content="LOGIN" Margin="0 0 60 0"/>
                    <Button x:Name="Register" Style="{StaticResource RoundButtonStyle}" Width="140" Height="50" Content="REGISTER"/>
                </StackPanel>

            </StackPanel>





        </Grid>
    </Grid>
</UserControl>
