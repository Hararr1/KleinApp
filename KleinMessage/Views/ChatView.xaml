<UserControl x:Class="KleinMessage.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
           
             d:DesignHeight="710" d:DesignWidth="500" Foreground="Red">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1" Margin="10" Background="White" >
            <ScrollViewer Margin="20" Background="Transparent">
                <StackPanel Orientation="Vertical" Margin="10">
                    <ListView x:Name="MessageListBox">
                      
                    </ListView>
                
                </StackPanel>
            </ScrollViewer>
           
        </Grid>
        
        
        <DockPanel Grid.Column="1" Grid.Row="1" Margin="20 10 20 10" Background="White">
            <TextBox x:Name="MessageContent" Width="300" TextWrapping="Wrap"/>
            <Button x:Name="SendMessageButton" Content="Send" DockPanel.Dock="Right"/>
        </DockPanel>



    </Grid>
</UserControl>
